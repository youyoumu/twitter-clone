<%= form_with model: tweet, local: true do |f|%>
  <%= f.fields_for :parent, parent do |parent_f| %>
    <%= parent_f.hidden_field :id, value: parent.id %>
  <% end %>
  <%= f.text_field :content %>
  <%= f.submit "Tweet" %>
<% end %>