<%= render  partial: "tweet_error", locals: {tweet: tweet} %>
<%= form_with model: tweet, local: true do |f|%>
  <%= f.text_field :content %>
  <%= f.file_field :tweet_pic %>
  <%= f.submit "Tweet" %>
<% end %>